---
--- 
<html lang="it">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Eigen.lab - Lavori in corso</title>
    <style>
      /* Reset base */
      * { margin: 0; padding: 0; box-sizing: border-box; }

      body {
        font-family: "Inter", system-ui, sans-serif;
        background: linear-gradient(135deg, #0c1124, #10162b, #05070c);
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        color: #ffffff;
      }

      .container {
        text-align: center;
        padding: 2rem 3rem;
        background: rgba(255,255,255,0.03);
        backdrop-filter: blur(12px);
        border-radius: 14px;
        box-shadow: 0 0 20px rgba(0,0,0,0.6);
      }

      h1 {
        font-size: 2rem;
        margin-bottom: 1rem;
        color: #ffb636;
        letter-spacing: 0.1em;
      }

      p {
        font-size: 1rem;
        opacity: 0.85;
        margin-bottom: 1.5rem;
      }

      .emoji {
        font-size: 2.2rem;
        display: inline-block;
        margin-bottom: 1rem;
      }

      .note {
        font-size: 0.9rem;
        opacity: 0.6;
      }

      @media (max-width: 600px) {
        h1 { font-size: 1.6rem; }
        p { font-size: 0.95rem; }
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="emoji">🚧</div>
      <h1>Lavori in corso</h1>
      <p>
        Questa parte del sito è ancora in fase di sviluppo, ma sarà presto disponibile.
      </p>
      <p class="note">
        Grazie per la pazienza 🛠️
      </p>
    </div>
  </body>
</html>
